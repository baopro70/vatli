const missions=[{name:'Ổn định bình O₂',npc:'Mira · Kỹ sư hệ thống',room:'CREW QUARTERS',brief:'Bình oxy quá áp đang rung mạnh. Đóng van làm mát và xả áp theo đúng thứ tự.',steps:['MỞ VAN LÀM MÁT','XẢ ÁP SUẤT','KHÓA VAN AN TOÀN'],explain:'Bạn đã làm giảm T rồi giảm n trong bình kín: P = nRT/V.',impact:[8,0],type:'valve'},{name:'Giải cứu khoang B',npc:'Jon · Phi hành gia',room:'AIRLOCK',brief:'Cửa cứu hộ bị kẹt. Hãy bơm piston đủ 5 nhịp để tạo chênh lệch áp suất đẳng nhiệt.',steps:['BƠM PISTON'],explain:'Mỗi nhịp làm V giảm, vì vậy P tăng theo PV = hằng số.',impact:[-5,-12],type:'pump'},{name:'Bịt rò rỉ oxy',npc:'Sana · Bác sĩ trưởng',room:'ENGINEERING',brief:'Ba điểm rò đang làm khoang mất oxy. Di chuyển công cụ hàn tới từng điểm và niêm phong chúng.',steps:['ĐIỂM RÒ A','ĐIỂM RÒ B','ĐIỂM RÒ C'],explain:'Cô lập khoang làm giảm V cần duy trì; bổ sung O₂ làm tăng n.',impact:[18,-8],type:'leak'},{name:'Khởi động lõi Helios',npc:'Mira · Kỹ sư hệ thống',room:'REACTOR CORE',brief:'Lõi đang quá nóng. Thực hiện chuỗi thao tác: làm lạnh, mở thể tích đệm, rồi khóa áp suất.',steps:['BẬT LÀM LẠNH','MỞ THỂ TÍCH ĐỆM','KHÓA ÁP SUẤT'],explain:'Theo P ∝ T/V, giảm T và tăng V đưa lõi về vùng an toàn.',impact:[0,-10],type:'core'}];
const canvas=document.querySelector('canvas'),ctx=canvas.getContext('2d'),keys={},boards=[[145,135],[735,135],[180,405],[440,405]],npcs=[{x:90,y:150,name:'MIRA',color:'#55e6e0'},{x:790,y:150,name:'JON',color:'#ff8e70'},{x:90,y:405,name:'SANA',color:'#c79cff'},{x:790,y:405,name:'MIRA',color:'#55e6e0'}];let state,player,timer,stars=[],$=id=>document.getElementById(id);
function reset(){state={i:0,o:78,p:64,h:100,t:294,time:360,score:0,open:false,progress:0};player={x:440,y:270};stars=Array.from({length:90},()=>({x:Math.random()*900,y:Math.random()*540,r:Math.random()*2}));clearInterval(timer);timer=setInterval(()=>{if(!state.open){state.time--;state.o-=.03;state.t+=.01;hud();if(state.time<1||state.o<1)end(false)}},1000);renderTasks();hud()}
function hud(){[['oxygen','o'],['power','p'],['hull','h']].forEach(a=>{$(a[0]).textContent=Math.max(0,Math.round(state[a[1]]))+'%';$(a[0]+'Bar').style.width=Math.max(0,state[a[1]])+'%'});$('temp').textContent=Math.round(state.t)+' K';$('clock').textContent=String(Math.floor(Math.max(0,state.time)/60)).padStart(2,'0')+':'+String(Math.max(0,state.time)%60).padStart(2,'0')}
function renderTasks(){$('count').textContent=`0${Math.min(state.i+1,4)} / 04`;$('tasks').innerHTML=missions.map((m,i)=>`<div class="task ${i===state.i?'active ':''}${i<state.i?'done':''}"><b>${i<state.i?'✓':'0'+(i+1)}</b>${m.name}<br><small>${m.npc}</small></div>`).join('')}
function draw(){ctx.fillStyle='#071329';ctx.fillRect(0,0,900,540);stars.forEach(a=>{ctx.fillStyle='#6e91c7';ctx.fillRect(a.x,a.y,a.r,a.r)});ctx.strokeStyle='#203b68';ctx.lineWidth=3;ctx.strokeRect(25,25,850,490);ctx.fillStyle='#0c1e3e';ctx.fillRect(55,65,790,145);ctx.fillRect(55,285,790,170);ctx.fillStyle='#7893bc';ctx.font='11px monospace';ctx.fillText('CREW QUARTERS',75,90);ctx.fillText('AIRLOCK',735,90);ctx.fillText('ENGINEERING',75,310);ctx.fillText('REACTOR CORE',355,310);boards.forEach((b,i)=>{ctx.fillStyle=i<state.i?'#65e6a2':i===state.i?'#ffc857':'#536c99';ctx.shadowBlur=i===state.i?18:0;ctx.shadowColor=ctx.fillStyle;ctx.fillRect(b[0]-18,b[1]-15,36,30);ctx.shadowBlur=0;ctx.fillStyle='#081329';ctx.fillText(i<state.i?'✓':i===state.i?'!':'—',b[0]-3,b[1]+4)});npcs.forEach((n,i)=>{ctx.fillStyle=n.color;ctx.shadowColor=n.color;ctx.shadowBlur=i===state.i?15:0;ctx.beginPath();ctx.arc(n.x,n.y-20,11,0,7);ctx.fill();ctx.shadowBlur=0;ctx.fillRect(n.x-13,n.y-8,26,24);ctx.fillStyle='#d9f8ff';ctx.fillRect(n.x-8,n.y-22,16,6);ctx.fillStyle='#a8b9d4';ctx.font='9px monospace';ctx.fillText(n.name,n.x-20,n.y+30)});ctx.fillStyle='#55e6e0';ctx.shadowBlur=12;ctx.shadowColor='#55e6e0';ctx.beginPath();ctx.arc(player.x,player.y-20,11,0,7);ctx.fill();ctx.shadowBlur=0;ctx.fillRect(player.x-13,player.y-8,26,24);ctx.fillStyle='#c3fbff';ctx.fillRect(player.x-8,player.y-22,16,6);requestAnimationFrame(draw)}
function move(){let dx=(keys.d||keys.ArrowRight)-(keys.a||keys.ArrowLeft),dy=(keys.s||keys.ArrowDown)-(keys.w||keys.ArrowUp);if(dx||dy){let n=Math.hypot(dx,dy);player.x=Math.max(45,Math.min(855,player.x+dx/n*3));player.y=Math.max(48,Math.min(485,player.y+dy/n*3))}let b=boards[state.i],near=Math.hypot(player.x-b[0],player.y-b[1])<65,n=npcs[state.i],nearNpc=Math.hypot(player.x-n.x,player.y-n.y)<65;$('hint').textContent=near||nearNpc?'Tương tác với NPC / bảng nhiệm vụ — nhấn E để hành động.':'WASD / phím mũi tên: di chuyển · Tìm NPC và bảng vàng';if((near||nearNpc)&&keys.e&&!state.open)openTask()}
function openTask(){state.open=true;let m=missions[state.i];$('modalKicker').textContent='NPC COMMUNICATION · '+m.npc;$('title').textContent=m.name;$('desc').textContent=m.brief;$('actionArea').innerHTML=`<div class="action-box"><h2>THỰC HIỆN NHIỆM VỤ — ${m.room}</h2><p>Không có câu hỏi trắc nghiệm. Hãy thao tác trực tiếp với hệ thống:</p><div id="actionButtons"></div><div class="meter"><i id="actionMeter"></i></div><div id="feedback" class="feedback"></div></div>`;renderActions(m);$('modal').classList.remove('off')}
function renderActions(m){let box=$('actionButtons');if(m.type==='pump')box.innerHTML='<button class="action-btn" id="pump">BƠM PISTON <b>0 / 5</b></button>';else if(m.type==='leak')box.innerHTML=m.steps.map((s,i)=>`<button class="action-btn leak" data-step="${i}">🔧 ${s}</button>`).join('');else box.innerHTML=m.steps.map((s,i)=>`<button class="action-btn sequence" data-step="${i}">${i+1}. ${s}</button>`).join('');document.querySelectorAll('.sequence').forEach(b=>b.onclick=()=>sequence(+b.dataset.step,m));document.querySelectorAll('.leak').forEach(b=>b.onclick=()=>leak(+b.dataset.step,m));if(m.type==='pump')$('pump').onclick=()=>{state.progress++;$('pump').innerHTML=`BƠM PISTON <b>${state.progress} / 5</b>`;$('actionMeter').style.width=state.progress*20+'%';if(state.progress>=5)complete(m)}}
function sequence(i,m){if(i!==state.progress){$('feedback').textContent='Thứ tự chưa đúng. Kiểm tra bảng điều khiển và thử lại.';state.h-=4;hud();return}document.querySelectorAll('.sequence')[i].classList.add('done');state.progress++;$('actionMeter').style.width=state.progress/m.steps.length*100+'%';if(state.progress===m.steps.length)complete(m)}function leak(i,m){let b=document.querySelectorAll('.leak')[i];if(b.disabled)return;b.disabled=true;b.classList.add('done');state.progress++;$('actionMeter').style.width=state.progress/3*100+'%';if(state.progress===3)complete(m)}
function complete(m){state.score+=250;state.o+=m.impact[0];state.p+=m.impact[1];$('feedback').innerHTML=`✓ <b>HOÀN TẤT.</b> ${m.explain}<button class="next" id="next">TIẾP TỤC →</button>`;$('next').onclick=()=>{state.open=false;$('modal').classList.add('off');state.i++;state.progress=0;if(state.i===missions.length)end(true);else{renderTasks();$('crewStatus').textContent='Nhiệm vụ mới đã được ghi vào quest log.'}};hud()}
function end(win){clearInterval(timer);state.open=true;$('endTitle').textContent=win?'TRẠM ĐÃ ĐƯỢC ỔN ĐỊNH':'TÍN HIỆU HELIOS ĐÃ MẤT';$('endText').textContent=win?'Bạn đã nói chuyện với phi hành đoàn, hoàn thành từng quest và cứu trạm bằng hành động thực tế.':'Thân trạm mất ổn định. Hãy thử lại và thao tác cẩn thận hơn.';$('score').textContent='FINAL SCORE: '+state.score;$('end').classList.remove('off')}addEventListener('keydown',e=>{keys[e.key]=1;if(e.key==='e')e.preventDefault()});addEventListener('keyup',e=>keys[e.key]=0);$('reset').onclick=reset;$('again').onclick=()=>{$('end').classList.add('off');reset()};$('close').onclick=()=>{state.open=false;$('modal').classList.add('off')};setInterval(move,16);reset();draw();